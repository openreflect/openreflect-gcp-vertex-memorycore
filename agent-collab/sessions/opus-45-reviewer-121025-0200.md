# Opus-4.5 — Pre-Deploy Review 121025-0200

## Session Summary

Comprehensive codebase analysis identifying issues that could prevent successful deployment to Cloud Run and integration with ChatGPT web MCP.

---

## Critical Fixes Required

### 1. Test Scripts Missing Authorization Headers

**Severity:** 🔴 CRITICAL — Tests will fail with 401

**Files Affected:**
- `mcp-server-python/tests/test_local_docker.sh`
- `mcp-server-python/tests/test_http_server.py`

**Issue:**
The `_authorize()` function in `server_http.py` now denies ALL requests when `CONNECTOR_BEARER_TOKEN` is not set, returning 401. Test scripts don't pass this env var or include Authorization headers.

**Fix for `test_local_docker.sh`:**
```bash
#!/bin/bash
set -e

# ADD THIS: Define test token
TEST_TOKEN="test-local-token-123"

echo "Building Docker image locally..."
docker build -t vertex-memory-bank-mcp:local .

echo "Starting container..."
CONTAINER_ID=$(docker run -d \
  -p 8080:8080 \
  -e GOOGLE_CLOUD_PROJECT=directed-asset-479716-f6 \
  -e GOOGLE_CLOUD_LOCATION=us-central1 \
  -e CONNECTOR_BEARER_TOKEN=$TEST_TOKEN \
  -e PORT=8080 \
  vertex-memory-bank-mcp:local)

echo "Container started with ID: $CONTAINER_ID"
echo "Waiting for container to be ready..."
sleep 5

echo "Testing health endpoint..."
curl -f http://localhost:8080/health || echo "Health check failed"

echo "Testing root endpoint..."
curl -f http://localhost:8080/ || echo "Root endpoint failed"

echo "Testing SSE endpoint with initialize message..."
curl -X POST http://localhost:8080/sse \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TEST_TOKEN" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-12-01","capabilities":{},"clientInfo":{"name":"test","version":"1.0"}}}' \
  --max-time 10 || echo "SSE endpoint test failed"

echo "Testing /message endpoint..."
curl -X POST http://localhost:8080/message \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TEST_TOKEN" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/list","params":{}}' || echo "Message endpoint test failed"

echo "Container logs:"
docker logs $CONTAINER_ID

echo "Stopping container..."
docker stop $CONTAINER_ID
docker rm $CONTAINER_ID

echo "Local testing complete!"
```

**Fix for `test_http_server.py`:**
Add `headers` parameter to all httpx requests:
```python
# Add at top of file:
import os

# Get token from env or use default for testing
AUTH_TOKEN = os.getenv("TEST_BEARER_TOKEN", "test-token-123")
AUTH_HEADERS = {"Authorization": f"Bearer {AUTH_TOKEN}"}

# Then update each test function to include headers in POST requests
```

---

### 2. Duplicate google-genai in requirements.txt

**Severity:** 🟠 HIGH — Could cause version conflicts

**File:** `mcp-server-python/requirements.txt`

**Issue:**
Lines 5 and 10 both specify google-genai with different version requirements:
- Line 5: `google-genai>=1.40.0`
- Line 10: `google-genai>=0.1.0`

**Fix:**
Remove line 10 (`google-genai>=0.1.0`). Keep only the higher version spec.

**Corrected requirements.txt:**
```
# Core MCP and Memory Bank dependencies
mcp[cli]>=1.0.0
google-cloud-aiplatform>=1.118.0
vertexai>=1.0.0
google-genai>=1.40.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Additional dependencies for Gemini integration and ADK
google-adk>=1.15.1

# HTTP server dependencies for Cloud Run
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0

# Jupyter notebook support (optional)
jupyter>=1.0.0
ipykernel>=6.0.0
```

---

### 3. pyproject.toml Missing HTTP Dependencies (Optional)

**Severity:** 🟡 MEDIUM — Only affects `pip install .` users

**File:** `mcp-server-python/pyproject.toml`

**Issue:**
Dependencies list doesn't include `fastapi`, `uvicorn`, `httpx` needed for HTTP mode.

**Fix:**
```toml
[project]
name = "vertex-ai-memory-bank-mcp"
version = "0.1.0"
description = "Simple MCP server for Vertex AI Memory Bank"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0.0",
    "google-cloud-aiplatform>=1.118.0",
    "google-genai>=1.40.0",
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
    "httpx>=0.25.0",
]
```

---

## Pre-Deployment Checklist

### Environment Variables (Must Set in Cloud Run)

| Variable | Required | Description |
|----------|----------|-------------|
| `GOOGLE_CLOUD_PROJECT` | ✅ Yes | GCP project ID (e.g., `directed-asset-479716-f6`) |
| `GOOGLE_CLOUD_LOCATION` | ⚪ Optional | Defaults to `us-central1` |
| `AGENT_ENGINE_NAME` | ✅ Yes | Full resource path to Vertex AI Agent Engine |
| `CONNECTOR_BEARER_TOKEN` | ✅ Yes | Bearer token for ChatGPT auth |

### GCP Prerequisites

| Requirement | Status |
|-------------|--------|
| Service Account created | ⏳ Verify |
| SA has `roles/aiplatform.user` | ⏳ Verify |
| Agent Engine exists in Vertex AI | ⏳ Verify |
| Docker image built and pushed | ⏳ Run `./deploy/build.sh` |

### Validation Commands

After deployment, run these to verify:

```bash
# 1. Health check (no auth needed)
curl -i https://SERVICE_URL/health

# 2. Root endpoint (no auth needed)
curl -i https://SERVICE_URL/

# 3. SSE handshake (auth required)
curl -N -H "Authorization: Bearer YOUR_TOKEN" https://SERVICE_URL/sse

# 4. Initialize MCP (auth required)
curl -X POST https://SERVICE_URL/message \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}'

# 5. List tools (auth required)
curl -X POST https://SERVICE_URL/message \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/list","params":{}}'
```

---

## Verified Working

| Component | Status |
|-----------|--------|
| MCP content envelope format | ✅ Correct |
| SSE trailing slash `/sse/` | ✅ Implemented |
| Protocol version `2024-12-01` | ✅ Updated |
| Agent Engine loading on startup | ✅ Implemented |
| Dockerfile configuration | ✅ Correct |
| Root `/` endpoint | ✅ Implemented |
| Bearer token auth | ✅ Implemented |

---

## Summary

**3 fixes required before deployment:**
1. 🔴 Update `test_local_docker.sh` with auth token/headers
2. 🟠 Remove duplicate `google-genai` from `requirements.txt`
3. 🟡 Optional: Add HTTP deps to `pyproject.toml`

**Then proceed with:**
1. Build image: `./deploy/build.sh`
2. Deploy to Cloud Run with env vars set
3. Configure ChatGPT with SSE URL + bearer token
4. Test with validation commands

---

## References

- Gotchas analysis: `agent-collab/reviews/opus-45-gotchas-analysis-120925-2100.md`
- Critical fixes strategy: `agent-collab/decisions/opus-45-critical-fixes-strategy-121025-0130.md`
- Auth strategy: `agent-collab/decisions/opus-45-auth-strategy-120925-2030.md`


